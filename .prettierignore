# Formatter ignore rules, shared by oxfmt (primary) and prettier (if ever
# re-introduced). See `oxfmt --help` — oxfmt reads `.prettierignore` and
# `.gitignore` automatically when no `--ignore-path` is supplied.

# Golden byte-identical selftest fixtures for the research pipeline. These
# are snapshots of exact bytes the code emits and must NOT be reformatted;
# doing so drifts them away from what the code produces and breaks
# `tests/lib/node/pi/research-selftest.spec.ts`. Matches the same ignore
# rule already set in `.markdownlint-cli2.jsonc`.
lib/node/pi/research-selftest/fixtures/

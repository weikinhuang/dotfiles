/**
 * USAGE text for the `/subdir-agents` command. Pure string module so the
 * extension shell and the `--help` path share one source of truth.
 */
export const SUBDIR_AGENTS_USAGE = [
  'Usage: /subdir-agents',
  '',
  'Show AGENTS.md / CLAUDE.md files discovered in subdirectories this session.',
].join('\n');

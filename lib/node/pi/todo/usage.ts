/**
 * USAGE text for the `/todos` command. Pure string module so the
 * extension shell and the `--help` path share one source of truth.
 */
export const TODOS_USAGE = ['Usage: /todos', '', 'Show the current todo list for this branch.'].join('\n');

# Tool Preferences

## Web Fetching and Search

Always use the `fetch_web` MCP tools for fetching web pages and searching the web. Do NOT use the built-in `WebSearch`
or `WebFetch` tools. Prefer the MCP `fetch_web` tools in all cases.

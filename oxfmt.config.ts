import { defineConfig } from 'oxfmt';

export default defineConfig({
  arrowParens: 'always',
  printWidth: 120,
  proseWrap: 'always',
  semi: true,
  singleQuote: true,
  sortImports: true,
  trailingComma: 'all',
});

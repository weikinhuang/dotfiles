# SSH auto-completion based on entries in known_hosts.
if [[ -e "${HOME}/.ssh/known_hosts" ]]; then
	complete -o default -W "$("${HOME}/.ssh/known_hosts" | sed 's/[, ].*//' | sort | uniq | grep -v '[0-9]')" ssh
fi